name: tanqiy
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.3
  http: ^1.6.0
  shared_preferences: ^2.5.5
  youtube_player_flutter: ^10.0.1
  flame: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/materi_bab.json
    - lib/assets/

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "lib/assets/Icon.jpeg"
